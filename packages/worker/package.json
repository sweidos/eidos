{
  "name": "@vardi/worker",
  "version": "0.1.0",
  "description": "Vardi service worker source (compiled output ships with the vardi package)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.3.1"
  }
}
