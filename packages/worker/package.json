{
  "name": "@eidos/worker",
  "version": "0.1.0",
  "description": "Eidos service worker source (compiled output ships with the eidos package)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.3.1"
  }
}
